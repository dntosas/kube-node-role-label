# kube-node-role-label

![Version: 1.1.0](https://img.shields.io/badge/Version-1.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.1.0](https://img.shields.io/badge/AppVersion-1.1.0-informational?style=flat-square)

Derives node-role.kubernetes.io/* labels from existing node labels so `kubectl get nodes` shows meaningful roles.

## Installation

```console
helm repo add kube-node-role-label https://dntosas.github.io/kube-node-role-label/
helm repo update
helm upgrade --install kube-node-role-label kube-node-role-label/kube-node-role-label \
  --namespace kube-utils --create-namespace \
  --set label_watch.labels=karpenter.sh/nodepool
```

## Logging

The controller logs structured JSON by default (`logFormat: json`). At the
default `logLevel: info` a cluster that is already fully labelled produces no
output at all; each added role is logged exactly once. Set `logLevel: debug`
to see every node visited.

## Requirements

Kubernetes: `>=1.25.0-0`

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` | Affinity rules for pod scheduling. |
| extraArgs | list | `[]` | Additional command-line arguments appended to the container args. |
| fullnameOverride | string | `""` | Override the fully qualified release name. |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy. |
| image.repository | string | `"ghcr.io/dntosas/kube-node-role-label"` | Container image repository. |
| image.tag | string | `""` | Image tag. Defaults to the chart appVersion. |
| imagePullSecrets | list | `[]` | Secrets for pulling the image from a private registry. |
| label_watch.interval | string | `"5m"` | How often to reconcile. Node labels rarely change after a node joins, so 5m-15m is plenty. Accepts Go durations (30s, 5m, 1h). |
| label_watch.labels | string | `"node-type"` | Comma-separated node label keys to watch. For every node carrying `key=value`, `node-role.kubernetes.io/<value>=true` is added. |
| logFormat | string | `"json"` | Log format: json or text. |
| logLevel | string | `"info"` | Minimum log level: debug, info, warn or error. At `info` a steady-state cluster produces no output; every added role is logged once. |
| nameOverride | string | `""` | Override the chart name. |
| nodeSelector | object | `{}` | Node selector for pod scheduling. |
| podAnnotations | object | `{}` | Annotations added to the pod. |
| podLabels | object | `{}` | Labels added to the pod (e.g. a `service` label for log routing). |
| podSecurityContext | object | `{"fsGroup":65532,"runAsGroup":65532,"runAsNonRoot":true,"runAsUser":65532,"seccompProfile":{"type":"RuntimeDefault"}}` | Pod-level security context. |
| priorityClassName | string | `""` | Priority class for the pod, e.g. `system-cluster-critical`. |
| rbac.create | bool | `true` | Create a ClusterRole/ClusterRoleBinding allowing list+patch on nodes. |
| replicaCount | int | `1` | Number of replicas. The controller is idempotent, but one replica is enough. |
| resources | object | `{"limits":{"memory":"64Mi"},"requests":{"cpu":"10m","memory":"32Mi"}}` | Resource requests and limits. The controller is tiny; these are generous. |
| revisionHistoryLimit | int | `3` | Number of old ReplicaSets to retain. |
| securityContext | object | `{"allowPrivilegeEscalation":false,"capabilities":{"drop":["ALL"]},"privileged":false,"readOnlyRootFilesystem":true}` | Container-level security context. |
| serviceAccount.annotations | object | `{}` | Annotations added to the ServiceAccount (e.g. for IRSA/Workload Identity). |
| serviceAccount.automountServiceAccountToken | bool | `true` | Mount the ServiceAccount token. Required for in-cluster API access. |
| serviceAccount.create | bool | `true` | Create a ServiceAccount for the controller. |
| serviceAccount.name | string | `""` | ServiceAccount name. Generated from the release name when empty. |
| tolerations | list | `[]` | Tolerations for pod scheduling. |
| topologySpreadConstraints | list | `[]` | Topology spread constraints for pod scheduling. |

---
Autogenerated from chart metadata by [helm-docs](https://github.com/norwoodj/helm-docs); edit `README.md.gotmpl` or `values.yaml` comments and run `make helm-docs`.
